const caixaprincipal = document.querySelector(".caixa-principal");
const caixapergunta = document.querySelector(".caixa-pergunta");
const caixaalternativa = document.querySelector(".caixa-alternativa");
const caixaresultado = document.querySelector(".caixa-resultado");

const perguntas = [
    {
        enunciado: "A IA substituirá completamente alguns trabalhos humanos?",
        alternativas: ["Sim", "Não"]
    },
    {
        enunciado: "A IA será capaz de desenvolver consciência própria?",
        alternativas: ["Sim", "Não"]
    },
    {
        enunciado: "O uso da IA na educação será mais comum no futuro?",
        alternativas: ["Sim", "Não"]
    },
    {
        enunciado: "A IA representará um risco para a segurança global?",
        alternativas: ["Sim", "Não"]
    },
    {
        enunciado: "A IA poderá criar arte indistinguível da arte humana?",
        alternativas: ["Sim", "Não"]
    }
];

let indiceAtual = 0;
let historiaFinal = "";

function mostrarPergunta() {
    if (indiceAtual < perguntas.length) {
        const perguntaAtual = perguntas[indiceAtual];
        caixapergunta.textContent = perguntaAtual.enunciado;
        mostrarAlternativas(perguntaAtual);
    } else {
        mostrarResultado();
    }
}

function mostrarAlternativas(perguntaAtual) {
    caixaalternativa.innerHTML = ""; // limpa as alternativas anteriores
    perguntaAtual.alternativas.forEach((alternativa) => {
        const botao = document.createElement("button");
        botao.textContent = alternativa;
        botao.classList.add("botao-alternativa");
        botao.onclick = () => respostaSelecionada(alternativa);
        caixaalternativa.appendChild(botao);
    });
}

function respostaSelecionada(opcao) {
    historiaFinal += `Você respondeu "${opcao}" à pergunta ${indiceAtual + 1}. `;
    indiceAtual++;
    mostrarPergunta();
}

function mostrarResultado() {
    caixapergunta.textContent = "O futuro chegou... 🌍";
    caixaalternativa.innerHTML = "";
    caixaresultado.textContent = historiaFinal + "Como você moldará o amanhã?";
    caixaresultado.classList.add("mostrar");
}

mostrarPergunta();
